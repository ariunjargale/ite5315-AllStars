<div class="episode-detail-container">
    <div class="episode-detail-header">
        <h1>{{episode.name}}</h1>
        <span class="episode-code-large">{{episode.episode}}</span>
    </div>

    <div class="episode-info">
        <div class="info-row">
            <span class="info-label">Air Date:</span>
            <span class="info-value">{{episode.air_date}}</span>
        </div>
        <div class="info-row">
            <span class="info-label">Episode ID:</span>
            <span class="info-value">{{episode.episodeId}}</span>
        </div>
        <div class="info-row">
            <span class="info-label">Total Characters:</span>
            <span class="info-value">{{episode.characters.length}}</span>
        </div>
    </div>

    <div class="characters-section">
        <h2>Characters in this Episode</h2>
        <div class="character-ids">
            {{#each episode.characters}}
            <span class="character-id-badge">{{this}}</span>
            {{/each}}
        </div>
    </div>

    <div class="episode-actions">
        <a href="/episodes" class="btn-back">← Back to Episodes</a>
    </div>
</div>

<style>
    .episode-detail-container {
        max-width: 900px;
        margin: 0 auto;
        padding: 20px;
    }

    .episode-detail-header {
        background: linear-gradient(135deg, #00b5cc, #009aad);
        color: white;
        padding: 30px;
        border-radius: 12px;
        margin-bottom: 30px;
        text-align: center;
    }

    .episode-detail-header h1 {
        margin: 0 0 10px 0;
        font-size: 2em;
    }

    .episode-code-large {
        display: inline-block;
        background: rgba(255, 255, 255, 0.2);
        padding: 8px 20px;
        border-radius: 6px;
        font-size: 1.2em;
        font-weight: bold;
    }

    .episode-info {
        background: white;
        border: 1px solid #ddd;
        border-radius: 8px;
        padding: 25px;
        margin-bottom: 30px;
    }

    .info-row {
        display: flex;
        justify-content: space-between;
        padding: 12px 0;
        border-bottom: 1px solid #eee;
    }

    .info-row:last-child {
        border-bottom: none;
    }

    .info-label {
        font-weight: bold;
        color: #555;
    }

    .info-value {
        color: #333;
    }

    .characters-section {
        background: white;
        border: 1px solid #ddd;
        border-radius: 8px;
        padding: 25px;
        margin-bottom: 30px;
    }

    .characters-section h2 {
        margin-top: 0;
        color: #00b5cc;
        border-bottom: 2px solid #00b5cc;
        padding-bottom: 10px;
    }

    .character-ids {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-top: 20px;
    }

    .character-id-badge {
        background: #f0f0f0;
        border: 1px solid #ccc;
        padding: 6px 12px;
        border-radius: 4px;
        font-size: 0.9em;
        color: #555;
        transition: all 0.2s;
    }

    .character-id-badge:hover {
        background: #00b5cc;
        color: white;
        border-color: #00b5cc;
        cursor: pointer;
    }

    .episode-actions {
        text-align: center;
    }

    .btn-back {
        display: inline-block;
        background: #666;
        color: white;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 6px;
        transition: background 0.2s;
    }

    .btn-back:hover {
        background: #555;
    }
</style>